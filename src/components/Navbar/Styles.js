export const styleNavbar = (theme) => ({
  display: "flex",
  flexWrap: "wrap",
  alignItems: "center",
  justifyContent: "space-between",
  width: "100%",
});

export const styleNavbarTabs = (theme) => ({
  display: "flex",
  alignItems: "center",
  padding: "2px",
  overflow: "hidden",
});
